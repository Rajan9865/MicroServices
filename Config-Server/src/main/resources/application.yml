#spring.application.name=Config-Serve
server:
  port: 8085
spring:
  application:
    name: Config-Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rajan9865/microservice-config
          clone-on-start: true

